################################################################################
# -- Design Specific Rules --
################################################################################

# (processor) Ignore Quartus generated metadata and output files.
processor/**/db
processor/**/incremental_db
processor/**/output_files
processor/**/*.qws
processor/**/*.bak
processor/**/src/*.bak

# (processor) Ignore bins created for testing hardware.
processor/tools/tests/**/*.bin

# (compiler) Ignore artifacts of "asmld" building/testing/developing.
compiler/asmld/asmld.exe
compiler/asmld/build
compiler/asmld/AsmldProject/Debug
compiler/asmld/AsmldProject/.settings

# (compiler) Ignore artifacts of "irbend" building/testing/developing.
compiler/irbend/irbend.exe
compiler/irbend/build
compiler/irbend/IrbendProject/Debug
compiler/irbend/IrbendProject/.settings

# (compiler) Ignore Eclipse generated/related files (except for makefile).
compiler/AsmLdProject/.settings
compiler/AsmLdProject/Debug/*
!compiler/AsmLdProject/Debug/makefile

# (compiler) Ignore CppCheck related items (bar one to ensure folder stays).
compiler/AsmLdProject/cppCheckDir/*
!compiler/AsmLdProject/cppCheckDir/files.txt

# (compiler) Ignore .gitignore in compiler project(s)- not needed.
compiler/*/.gitignore

################################################################################
# -- General Rules --
################################################################################

# Ignore pycaches- just "scripts running scripts"
**/__pycache__
